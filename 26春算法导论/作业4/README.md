# 作业 4 选择算法

本目录比较两类线性选择算法。

| 文件 | 说明 |
| --- | --- |
| `代码.py` | 实现 randomized select 和 median-of-medians 风格的 worst-case linear select，用于找第 k 大元素。 |
| `测评.py` | 对两个算法做正确性验证和运行时间比较。 |

运行：

```bash
python 测评.py
```
