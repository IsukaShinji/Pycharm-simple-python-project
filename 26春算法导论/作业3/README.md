# 作业 3 优先队列

本目录围绕堆和优先队列实现。

| 文件 | 说明 |
| --- | --- |
| `最小优先队列的实现.py` | 手写最小堆，支持 `minimum`、`extract_min`、`decrease_key` 和 `insert`。 |
| `优先队列先进先出.py` | 借助 `heapq` 用优先级模拟 FIFO 队列和 LIFO 栈。 |

运行脚本可看到堆数组或出队/出栈顺序。
