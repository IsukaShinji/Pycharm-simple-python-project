# Lab 6 Word2Vec

本目录是《人工智能引论》Word2Vec 实验的外层目录，实际代码和数据都放在 `w2v/` 子目录中。

## 目录结构

| 目录或文件 | 说明 |
| --- | --- |
| `w2v/` | Word2Vec 训练、梯度检查、数据工具和训练结果。 |
| `__init__.py` | Python 包标记文件。 |

## 实验内容

实验重点是手写 Skip-gram Word2Vec 的核心计算过程，包括 sigmoid、softmax 损失、负采样损失、梯度推导和 SGD 训练流程。

进入 `w2v/` 后可查看更具体的运行说明。
