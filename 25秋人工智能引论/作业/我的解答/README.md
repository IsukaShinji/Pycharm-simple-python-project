# 我的解答

本目录保存课程作业的个人实现，目前核心文件是 `main.py`。

## 文件说明

| 文件 | 说明 |
| --- | --- |
| `main.py` | 基于 `pandas`、`numpy`、`scikit-learn`、`scipy` 和多进程工具的建模脚本，包含数据读取、规则建模、MCMC 采样、特征构造和预测流程。 |

## 运行提示

```bash
python main.py
```

脚本中包含对训练集、测试集和原始数据路径的读取逻辑。运行前需要检查数据文件是否存在，以及路径是否符合当前环境。
